{
  "name": "rest-api-poc",
  "version": "1.0.0",
  "description": "REST API POC",
  "main": "server.js",
  "scripts": {
    "server": "nodemon ./src/server.js",
    "build": "esbuild ./src/* --bundle --minify --sourcemap --platform=node --target=node16.14 --outdir=dist",
    "lint": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.8.8",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "@types/aws-lambda": "^8.10.125",
    "esbuild": "^0.19.5"
  }
}
